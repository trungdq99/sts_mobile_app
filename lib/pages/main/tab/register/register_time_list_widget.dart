/*
 * Author: Trung Shin
 */

import 'package:flutter/material.dart';

class RegisterTimeListWidget extends StatelessWidget {
  const RegisterTimeListWidget({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
